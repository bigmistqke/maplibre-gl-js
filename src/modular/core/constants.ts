/** Tile size in CSS pixels at native zoom — matches MapLibre's vector tile default (512). */
export const TILE_SIZE = 512

/** Tile coordinate extent — vector tiles use [0, 4096] for vertex coordinates. */
export const TILE_EXTENT = 4096
